{
  "name": "poc-ado-to-gha-migration",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "projects/subscription-portal"
  ],
  "scripts": {
    "build": "npm run build --workspace projects/subscription-portal",
    "lint": "npm run lint --workspace projects/subscription-portal",
    "validate:shell": "bash -n projects/provisioning-scripts/scripts/*.sh",
    "validate": "npm run lint && npm run build && npm run validate:shell"
  }
}
